"use client";
import type { TradeSession } from "./types";
export function TradeWorkspaceSessionList({ sessions, selectedId, onSelect }: { sessions: TradeSession[]; selectedId: string | null; onSelect: (id: string) => void }) {
  return <aside aria-label="Daftar sesi" className="min-w-0 rounded-xl border border-zinc-200 bg-white p-3 shadow-sm"><h2 className="px-2 pb-2 text-sm font-semibold">Sesi Saya</h2>{sessions.length === 0 ? <p className="px-2 text-sm text-zinc-500">Belum ada sesi.</p> : <div className="min-w-0 space-y-1">{sessions.map(s => <button key={s.id} onClick={() => onSelect(s.id)} className={`min-w-0 w-full rounded-lg p-3 text-left break-words ${selectedId === s.id ? "bg-blue-50 ring-1 ring-blue-200" : "hover:bg-zinc-50"}`}><span className="block min-w-0 break-words font-semibold">{s.ticker}</span><span className="block min-w-0 break-words text-sm text-zinc-600">{s.company_name}</span><span className="mt-1 block min-w-0 break-words text-xs text-zinc-500">{s.status}</span></button>)}</div>}</aside>;
}
